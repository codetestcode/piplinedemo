node {
      stage('Checkout'){
        sh 'echo code checkout'
      }
      stage('Lint') {
        sh 'echo lint source code'
      }
    stage('Build') {
        sh 'echo build'
    }

    stage('Test') {
        sh 'echo test'
    }

    stage('Deploy-Dev') {
        sh 'echo delpoy'
    }
    stage('Checkout'){
      sh 'echo code checkout'
    }
    stage('Lint') {
      sh 'echo lint source code'
    }
  stage('Build') {
      sh 'echo build'
  }

  stage('Test') {
      sh 'echo test'
  }

  stage('Deploy-QA') {
      sh 'echo delpoy'
  }
  stage('Checkout'){
    sh 'echo code checkout'
  }
  stage('Lint') {
    sh 'echo lint source code'
  }
stage('Build') {
    sh 'echo build'
}

stage('Test') {
    sh 'echo test'
}

stage('Deploy-Prod') {
    sh 'echo delpoy'
}
}
