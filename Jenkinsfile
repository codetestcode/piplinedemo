node {
      stage('Checkout'){
        sh 'echo code checkout'
      }
      stage('Lint') {
        sh 'echo lint source code'
      }
    stage('Build') {
        sh 'echo build'
    }

    stage('Test') {
        sh 'echo test'
    }

    stage('Deploy-Dev') {
        sh 'echo delpoy'
    }
    stage('Checkout-Dev'){
      sh 'echo code checkout'
    }
    stage('Lint-Dev') {
      sh 'echo lint source code'
    }
  stage('Build-Dev') {
      sh 'echo build'
  }

  stage('Test-QA') {
      sh 'echo test'
  }

  stage('Deploy-QA') {
      sh 'echo delpoy'
  }
  stage('Checkout-QA'){
    sh 'echo code checkout'
  }
  stage('Lint-QA') {
    sh 'echo lint source code'
  }
stage('Build-QA') {
    sh 'echo build'
}

stage('Test-QA') {
    sh 'echo test'
}

stage('Deploy-Prod') {
    sh 'echo delpoy'
}
}
