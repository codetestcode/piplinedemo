node {
      stage('Checkout'){
        sh 'echo code checkout'
      }
      stage('Lint') {
        sh 'echo lint source code'
      }
    stage('Build') {
        sh 'echo build'
    }

    stage('Test') {
        sh 'echo test'
    }

    stage('Deploy') {
        sh 'echo delpoy'
    }
}
