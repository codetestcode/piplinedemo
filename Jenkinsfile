node { node {
      stage('Checkout'){
        sh 'code checkout'
      }
      stage('Lint') {
        sh 'lint source code'
      }
      
    }
    stage('Build') {
        sh 'echo build'
    }

    stage('Test') {
        sh 'echo test'
    }

    stage('Deploy') {
        sh 'echo delpoy'
    }
}
